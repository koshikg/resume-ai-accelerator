{
    "name": "ResumeAI Accelerator DevContainer",
    "image": "mcr.microsoft.com/devcontainers/python:3.11",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra texlive-luatex latexmk ghostscript ca-certificates && pip install python-docx docx2txt && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter",
                "James-Yu.latex-workshop",
                "esbenp.prettier-vscode"
            ]
        }
    }
}